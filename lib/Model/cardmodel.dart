import 'dart:ui';

class card {
  String name;
  String address;
  String date;
  Color color;
  String state;
  card({this.name, this.address, this.state, this.color, this.date});
}

List<card> CardList = [];
